/**
 * 画布展示版本统一管理（003 补充）。
 *
 * 画布头部显示的语义版本号集中于此，任何展示位置引用本常量而非硬编码。
 * 注意：这是画布功能的语义版本（功能迭代计数），与 npm 包版本（package.json
 * version，发布节奏）相互独立——升级包版本时如需同步可在此一并更新。
 */
/** 画布功能语义版本（展示用，含 v 前缀）。 */
export const CANVAS_DISPLAY_VERSION = 'v3.3'
